#include <stdio.h>

int main()
{
    float a, b;

    printf("Print two not null number: \n");
    scanf("%f%f", &a, &b);
    printf("a + b = %f\n", a + b);
    printf("a - b = %f\n", a - b);
    printf("a * b = %f\n", a * b);
    printf("a / b = %f\n", a / b);

    return 0;
}
